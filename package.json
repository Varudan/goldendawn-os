{
  "name": "goldendawn",
  "version": "0.2.2",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "gateway:local": "node server/startLocalSyncGateway.js",
    "bundle:n8n:generate": "node scripts/n8n/generateSyncGatewayBoundaryBundle.js --write",
    "bundle:n8n:check": "node scripts/n8n/generateSyncGatewayBoundaryBundle.js --check",
    "probe:n8n:cloud:test": "node scripts/n8n/n8nCloudIngressProbe.js --run",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
